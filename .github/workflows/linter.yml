name: Check HTML

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: HTML Validate
      uses: cyisfor/html-validate-action@v1
      with:
        files: '*.html'