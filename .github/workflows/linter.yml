name: Check HTML

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: HTML Hint
      uses: htmlhint/htmlhint-action@v2
      with:
        file_pattern: '*.html'